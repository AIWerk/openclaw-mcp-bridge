{
  "name": "openclaw-mcp-bridge",
  "version": "1.5.0",
  "description": "MCP client plugin for OpenClaw — bridge to any MCP server",
  "license": "MIT",
  "repository": {
    "type": "git",
    "url": "https://github.com/AIWerk/openclaw-mcp-bridge"
  },
  "scripts": {
    "test": "node --import tsx --test tests/*.test.ts"
  },
  "dependencies": {
    "@sinclair/typebox": "^0.34.0"
  },
  "devDependencies": {
    "@types/node": "^25.3.5",
    "tsx": "^4.0.0"
  }
}
