#!/bin/bash
# Apify MCP Server — hosted service (Streamable HTTP), no local install needed
set -e
echo "=== Apify MCP Server Setup ==="
echo ""
echo "Apify is a hosted service — no local installation required!"
echo "Transport: Streamable HTTP (https://mcp.apify.com)"
echo ""
echo "Next steps:"
echo "1. Get your API token: https://console.apify.com/account/integrations"
echo "2. Add APIFY_TOKEN=apify_api_... to your .env file"
echo "3. Add the server config from config.json to your openclaw.json"
echo "4. Restart the gateway"
