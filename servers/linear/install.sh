#!/bin/bash
# Linear MCP Server installer — runs via npx (no install needed)
set -e
echo "=== Linear MCP Server Setup ==="
echo ""
echo "This server runs via npx — no installation required!"
echo ""
echo "Next steps:"
echo "1. Get your API key: Linear → Settings → Account → Security & access → Personal API keys"
echo "2. Add LINEAR_API_KEY=your_key to your .env file"
echo "3. Add the server config from config.json to your openclaw.json"
echo "4. Restart the gateway"
