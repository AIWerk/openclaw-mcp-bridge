{
  "transport": "stdio",
  "command": "npx",
  "args": ["-y", "@anthropic-pb/stripe-mcp-server", "--tools=all", "--api-key=${STRIPE_API_KEY}"],
  "env": {
    "STRIPE_API_KEY": "${STRIPE_API_KEY}"
  }
}
